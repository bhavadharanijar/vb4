#include<stdio.h>
#include<string.h>

int main()
{
	char r[100],b[100] ;
	int i ,c=0; 
	gets(a);
	gets(b);
	
	for(i=0 ; r[i] !='\0' ; i++)
	{
		if( r[i] != b[i])
		{
			c++;
		}
	}
		if(c == 1)
		printf("Yes");
		else
		printf("No");
	return();
}
