#include <stdio.h>
using namespace std;
int main ()
{
 int a2[30],num;
 int i, n, a1, j;
 cin>>n;
 for (i = 0; i < n; ++i)
    cin>>a2[i]);
    cin>>a1;
 for (i = 0; i < a1; ++i)
 {
   a2[n] = a2[0];
   for (j = 0; j < n; ++j) 
    num[j] = num[j + 1];
 }

for (i = 0; i < n; ++i)
    cout<<a2[i];
}
