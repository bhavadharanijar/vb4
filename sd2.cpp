#include <bits/stdc++.h>
using namespace std;
int main ()
{
 int a[30];
 int i, n, a1, j;
 cin>>n;
 for (i = 0; i < n; ++i)
    cin>>a[i]);
    cin>>a1;
 for (i = 0; i < a1; ++i)
 {
   a[n] = a[0];
   for (j = 0; j < n; ++j) 
    number[j] = number[j + 1];
 }

for (i = 0; i < n; ++i)
    cout<<a[i];
}
